import { StyleSheet } from "react-native";

export default StyleSheet.create({
  wrapper: {
    height: 42,
    paddingHorizontal: 5,
    marginVertical: 5,
    borderRadius: 5,
    alignItems: "center",
    justifyContent: "space-evenly",
  },
  loadSection: {
    flexDirection: "row",
  },
});
